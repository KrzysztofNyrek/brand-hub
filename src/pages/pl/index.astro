---
// Hub PL — dostępny na „/pl/" w domyślnym buildzie (do preview/weryfikacji obu wariantów).
// Na produkcji .pl root = ten sam widok przez build HUB_LANG=pl (patrz README, index.astro).
import HubHome from '../../components/HubHome.astro';
---
<HubHome lang="pl" />
